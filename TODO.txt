

Look at 

  http://dean.edwards.name/download/#packer

  http://code.google.com/p/minify/

Examples of parsers

  JSon parser in Parsec
    http://snippets.dzone.com/posts/show/3660

GOLD Parser, using the Javascript.grm from
http://www.devincook.com/GOLDParser/grammars/index.htm

-------------------------

- Seems rOp consumes multiple of the same char.
  e.g (rOp ",") will consume one or more comma's.  Need to replace with something else

- Generate output using standard pretty print library, so it can be
  used with various backends

- Why does identifier consume everything after it?
  doParse identifier "ExpArray(1,1);}blahwhoohick"
  "Parsed:JSNode JS_value (JSIdentifier \"ExpArray\") [] [] []"

EOF
